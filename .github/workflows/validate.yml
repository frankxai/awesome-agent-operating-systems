name: Validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  links:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate links
        shell: pwsh
        run: ./scripts/validate-links.ps1
